let arr = ["one", "two", "three", "four", "five"]
for(let i = 0; i < arr.length; i++){
    if (arr[i]   === "one") {
        console.log(arr[i] + ":" + 1);
    }
    if (arr[i]   === "two") {
        console.log(arr[i] + ":" + 2);
    }
}
